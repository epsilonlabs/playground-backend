@namespace(uri="psl", prefix="")
package psl;

@Node(label="title", contents="tasks, people")
class Project {
	attr String title;
	attr String description;
	val Task[*] tasks;
	@diagram(direction="right")
	val Person[*] people;
}

@Node(label="eol:'<<Task>>\\n' + self.title", shape="node", style="fontcolor green")
class Task {
	attr String title;
	attr int start;
	attr int duration;
	@diagram(direction="right")
	val Effort[*] effort;
	@Edge(label="leads", color="red", pattern="dashed", thickness="2")
	ref Person lead;
}

@Node(label="name", color="azure", shape="cloud", style="fontcolor magenta, fontsize 20")
class Person {
	attr String name;
}

@Node(label="eol: 'Manager: ' + self.name", color="green", style="fontcolor white")
class Manager extends Person {

}

@Edge(source="person", target="eol:self.eContainer()", thickness="eolp: return self.percentage/10;", direction="down", label="eol:self.percentage < 100 ? self.percentage+'%' : ''")
class Effort {
    @diagram(direction="up")
	ref Person person;
	attr int percentage = 100;
}